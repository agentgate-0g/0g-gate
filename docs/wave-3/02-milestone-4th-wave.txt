Make the rail general, and enforce budgets on-chain.

1. ERC-20 payments. Services already store an on-chain accepts[] price list, so a second asset needs no registry change, only a router path that pulls an ERC-20 instead of forwarding msg.value. Done when one service quotes OG and a test ERC-20 in one 402 invoice, a buyer settles either, and both are attested.

2. SpendGuard in the request path. Deployed but uncalled: budgets are enforced client-side, so an agent polices its own limit. Done when a cap is checked on-chain before payment lands and an over-cap transaction reverts.

3. Hosted dashboard on 0G. agentgate.mdloglabs.org still runs an older build. Done when it reads the Galileo registry live: catalog, activity feed, attestation history.

4. Types that stand alone. The shipped .d.ts names ambient DOM types. Done when a project with lib ES2022 and no ambient types compiles.
