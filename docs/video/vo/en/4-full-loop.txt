The whole loop, end to end. Register, four-oh-two, pay, serve, attest, score. The buyer pays through the Payment Router contract, which binds that payment to the invoice nonce on-chain and rejects a replay. So the gateway verifies with one log lookup on the receipt. No scanning, no indexer, no facilitator, and the gateway never holds a cent. This run is on the mock chain, so it finishes in under a second. The same code path runs against zero-G Galileo.
