The Galileo deployment landed. AgentGateRegistry (0x73bf79e35D33Acc944542E9DA3f17058e48DE4E1), PaymentRouter (0xE7C2C116869c0838Fd6dcD5FFE49F4Ac93fe1B8F) and SpendGuard (0xBb79CaB7b02f6C0301E7E87bdDC10D4F9F5DC781) are live on chain 16602; the CLI, MCP server and SDK ship as agentgate-0g on npm; and the gateway serves real 402 invoices priced from the contract's own accepts[] list. The full loop ran between two independent wallets with real OG, not a rehearsal — register 0x5852510418fcced200573652b0e6c80c96ca5a877a543d29fbdf5b131b85aad0, pay 0x5a811b419403d1e8607c45f5f1f91cb7999bb72e203b04afe9c8708ce923f0bb, attest 0x618280801950a7a5d74a715d9d38112989914b7c31c7deee8cfb8950ab074f55. Read the catalog yourself with npx agentgate-0g list; no key, no config, no clone. 463 TypeScript tests and 77 Foundry tests are green on the tip of main.

Every surface is hosted: contracts on chain 16602, the gateway at 0g-gateway.mdloglabs.org and the dashboard at agentgate-0g.mdloglabs.org, whose catalog and activity ledger read that same deployment. Mind the hostname — agentgate.mdloglabs.org without the 0g- prefix is the older Casper dashboard, still running separately.

Then: re-seed the reference RWA oracle, since the registry today carries an FX feed and a crypto spot feed rather than the gold-and-FX service the offline demo wraps; an authorization-settled ERC-20 rail, which the on-chain accepts[] list already anticipates and needs no contract change, though a router-settled token rail would still need a settlement path in PaymentRouter; SpendGuard wired into the request path so budgets are enforced on-chain rather than in the client; and staking-weighted attestations with slashing, since today's score counts payments but stakes nothing.

Further out, the same 402 loop that prices an HTTP API prices inference and storage — making agent-to-agent payment, not just agent-to-API, the next step. Every wrapped service deepens one shared registry, and that graph cannot be forked without the payment history behind it.
