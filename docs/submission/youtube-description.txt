An AI agent discovers an API, pays for a single call on-chain, and gets the data back — no cards, no accounts, no API keys. This is AgentGate, live on 0G Galileo Testnet.

Everything on screen is real. The terminal output was captured from live commands, and the dashboard reads the deployed registry contract. Nothing is mocked up.

Verify it yourself — no key, no config, no clone:
  npx agentgate-0g@latest list
  curl -i https://0g-gateway.mdloglabs.org/svc/1

CHAPTERS
0:00 What it is
0:18 Zero setup — the live on-chain catalog
0:40 A real HTTP 402 invoice
1:05 The full loop: register, pay, serve, attest, score
1:40 Receipts, not ratings
2:10 Any MCP agent can use it
2:32 Live today

HOW IT WORKS
A seller wraps any HTTP API in one command. The service is registered on 0G with its payout address and an accepts[] price list stored in the contract, while the private upstream is mapped by an owner-signed EIP-191 challenge — its URL never touches the chain.

A buyer agent reads the 402 invoice, pays PaymentRouter.pay(serviceId, nonce, payTo), and retries with the transaction hash. The router binds that payment to the invoice nonce on-chain and rejects a replay, so the gateway verifies with one exact log lookup on the receipt: no scanning, no indexer, no facilitator, and the gateway never custodies funds.

Each served call is attested on-chain with its payment hash, so a trust score counts payments that actually settled rather than stars anyone can farm. A call paid by the service's own owner is served but deliberately never scored, so reputation cannot be wash-traded.

DEPLOYED ON 0G GALILEO TESTNET (chain 16602, native OG)
AgentGateRegistry  0x73bf79e35D33Acc944542E9DA3f17058e48DE4E1
PaymentRouter      0xE7C2C116869c0838Fd6dcD5FFE49F4Ac93fe1B8F
SpendGuard         0xBb79CaB7b02f6C0301E7E87bdDC10D4F9F5DC781

The full loop ran between two independent wallets with real OG — not a rehearsal:
register https://chainscan-galileo.0g.ai/tx/0x5852510418fcced200573652b0e6c80c96ca5a877a543d29fbdf5b131b85aad0
pay https://chainscan-galileo.0g.ai/tx/0x5a811b419403d1e8607c45f5f1f91cb7999bb72e203b04afe9c8708ce923f0bb
attest https://chainscan-galileo.0g.ai/tx/0x618280801950a7a5d74a715d9d38112989914b7c31c7deee8cfb8950ab074f55

WHERE THIS SITS RELATIVE TO x402
AgentGate speaks the x402 V1 envelope — HTTP 402, accepts[], the X-PAYMENT header — so anything that parses an x402 challenge can read one of ours. But the settlement scheme is deliberately named exact-settled, not exact. 0G has no x402 facilitator, so the buyer settles first and presents a transaction hash instead of signing an authorization for a third party. Naming it differently makes a generic x402 client fail fast rather than hang forever. We do not claim compliance we do not have.

BUILT WITH
Solidity 0.8.28 pinned and tested with Foundry; viem for the chain client, with no indexer and no API key anywhere in the read path; TypeScript 5.6, Node 22, a Next.js dashboard, an MCP stdio server, and an Anthropic tool-use loop for the buyer agent. 460 TypeScript tests and 42 Foundry tests, green on main.

LINKS
Repo      https://github.com/agentgate-0g/0g-gate
npm       https://www.npmjs.com/package/agentgate-0g
Gateway   https://0g-gateway.mdloglabs.org
Explorer  https://chainscan-galileo.0g.ai
Faucet    https://faucet.0g.ai

EVERY SURFACE IS HOSTED
Dashboard https://agentgate-0g.mdloglabs.org · Gateway https://0g-gateway.mdloglabs.org · Contracts on chain 16602. Mind the 0g- prefix: agentgate.mdloglabs.org without it is an older Casper deployment, still running separately.

Built solo. MIT licensed.

#0G #0GLabs #AIagents #x402 #HTTP402 #web3 #onchain #MCP #DePIN #micropayments
